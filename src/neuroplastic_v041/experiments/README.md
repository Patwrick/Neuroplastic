# Bounded lifecycle runner

`python -B -m neuroplastic_v041.experiments.smoke --output outputs/v0.4.1/new-run`
executes the shipped CPU config with fixed theta, two task draws, graph and delta
writes, full-graph sleep, empirical screening and cold recall. It preserves local
provenance and schema-validated results. It is not an M3 training command.
