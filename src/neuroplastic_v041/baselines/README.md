# Delta baseline

`DeltaModel` implements context-keyed normalized delta writes with an engineered
cue/context tensor-product key and the same causal feedback and slow-fit exposure
as the smoke graph. It is not a matched-memory or matched-latency comparison.
