# Confirmation preregistration — complete before opening confirmation data

Study ID / date / reviewer:
Paper version and package hash:
Implementation commit/diff hash:
Frozen config and data-split hashes:

## Primary hypothesis and comparison

H1/H2/H3; exact model/comparator; primary task/metric and its direction.

## Decision quantities

Minimum practically useful effect; tolerated still-valid old-task degradation; resource-ratio limits; valid sampling unit; interval/decision method; treatment of multiple comparisons. Values are chosen from scientific goals and development evidence, not confirmation outcomes.

## Data and compute

Independent training seeds; evaluation episodes per task/seed; held-out families; pilot variance/power rationale; total approved device/time budget; hyperparameter-search budget for every comparator; failure/abort handling.

## Frozen exclusions and controls

Leakage checks; cold recall; transplantation compatibility; erasure matching; negative controls; feasibility and gradient tests. State any technical exclusion rules before results. Never exclude a losing seed because its score is inconvenient.

## Execution authorization

Approved scope and upper budget:
Confirmation data still unopened: yes/no
Final plan digest:

A null/unfilled field is unresolved. Do not launch configs/confirmation_plan.json until this record is complete and approved.
