# CSGN v0.4.1 report-back

## 1. Identity and status

- Milestone / run ID:
- Status: planned | in_progress | completed | failed | aborted | blocked | inconclusive
- Evidence class: local_math | implementation_smoke | learned_pilot | independent_comparison | formal_conditional
- Repository / branch / commit / dirty diff hash:
- Package manifest / paper / config hashes:
- Date, environment, device, precision:

## 2. What actually changed

Changed files and purpose. Link contracts/equations. State reused legacy components. List uncommitted user work preserved. List any requested work not implemented.

## 3. Commands actually executed

| Command | Exit code | Duration | Log/artifact |
|---|---:|---:|---|

Do not include a planned command as executed. Include package checks and integration commands separately.

## 4. Tests

Counts passed/failed/skipped. Exact failure text. Gradient mode and boundary exclusions. Cold recall and leakage checks. Reference-versus-implementation parity. Explanation for unexecuted tests.

## 5. Results, controls and resources

Per-seed and per-task metrics; baselines; query/write/sleep/cold conditions; acceptance levels; confidence intervals and independent sampling units. Label missing metrics. Report training/inference/replay/validation time, memory bytes, data exposure and search budget.

## 6. Diagnostics

Endpoint violations; projection no-progress; local loss versus future write utility; reachability lower/upper bound and solver gap; rank/condition; address drift; pending-credit loss; false cold-recall shortcuts; transplant/erasure specificity when run.

## 7. Deviations and open specification questions

For each: ID, equation/section, expected behavior, actual behavior, minimal reproducer, options, proposed choice and effect on guarantees. Distinguish code bugs from scientific uncertainty.

## 8. Claim assessment

Which H1/H2/H3 claim is supported, contradicted, untested or inconclusive? What evidence specifically supports that label? What cannot be concluded? No global capability claims from local tests.

## 9. Next bounded action

One highest-information next experiment or repair. Exact runnable command after implementation, required resources, estimated upper budget, stopping conditions and what each outcome would teach us.

## 10. Artifact inventory

Paths and hashes for manifests, metrics, logs, model/split identifiers and optional checkpoint references. Confirm no secrets/private raw data in the shared report bundle.
